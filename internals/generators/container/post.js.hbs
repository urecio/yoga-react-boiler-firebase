{{#postAsyncRequests}}
/**
 * {{ ../name }} {{ properCase name }}
 * @return {object} The response data
 * @throws {Error} If response status is not valid
 */
function {{ ../name }}{{ properCase name }}(payload) {
  return request('{{ route }}', {
    method: 'POST',
    headers: {
      'Content-Type': 'application/json',
    },
    body: JSON.stringify(payload),
  }, false);
}
{{/postAsyncRequests}}

$1
